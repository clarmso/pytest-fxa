[tox]
envlist = py{310,311,312,313,314}, flake8

[testenv]
deps = -rpipenv.txt
commands =
    pipenv install --dev --skip-lock
    pipenv run pytest -v -r a {posargs}

[testenv:flake8]
skip_install = true
commands =
    pipenv install --dev --skip-lock
    pipenv run flake8
